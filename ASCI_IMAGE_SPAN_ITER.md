Create a helper in a new module (in span), which is only available for tests(cft(test)). It should work like AsciImageSpanIter::new(&[b"....#...#.", b"....#####.", ...]), (Accept a (const generic) Array of u8-arrays, which makes sure all rows have the same length... In the AsciImageSpanIter, store a Slice of slices to avoid godegen for all array sizes... Implement ImageDimension and Iterator<Item=Span> for it In the above case, it would generate a span with 3 items and have bounds=Rect(0,0, 10, 2)...
